# notebooks

Jupyter notebooks for exploration only. Anything reusable should be moved into
`src/` so it can be imported and tested.

Suggested naming convention: `<step>-<initials>-<description>.ipynb`
(e.g. `1.0-rk-initial-data-exploration.ipynb`).
